# Supabase UI reference

Drop the downloaded Supabase HTML (and any screenshots) here so I can study the
exact layout, spacing, component structure, and class patterns, then recreate
that quality in DoxaEd OMR.

Suggested filenames (so I know which screen each is):
- `org-projects.html`      — the Organization home (Projects grid)
- `org-team.html`          — Organization → Team (members)
- `org-settings.html`      — Organization → Settings / Billing / Usage
- `project-overview.html`  — a Project's Overview dashboard
- `project-sidebar.html`   — (optional) just the project left nav, if separable

Screenshots can go in `screens/` next to this file. Once a file is here, tell me
its name and I'll extract the structure and apply it to the matching DoxaEd page
(Org home, Class workspace, Team, Settings, etc.).
